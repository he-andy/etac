use io 
use conv 

add_mul(x:int, y:int):int, int {
  return x + y, x * y
}

mul(x:int, y:int):int {
  return x * y
}

main(args:int[][]) {
  x:int, y:int = add_mul(1, 2)
  println(unparseInt(x))
  println(unparseInt(y))
}