use io 
use conv 

main(args:int[][]) {
  x:bool[] = {true, true}
  y:bool[] = {false, false}
  z:bool[] = {true, false}
  w:bool[] = x + y + z 
  
  i:int = 0 
  while(i < length(w)) {
    if(w[i]) {
      println(unparseInt(1))
    } else {
      println(unparseInt(0))
    }
    i = i + 1
  }
}