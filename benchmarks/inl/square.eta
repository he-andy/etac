use io
use conv

function square(n:int) : int {
  return n * n
}

main(args:int[][]) {
  sum:int = 0
  for (i:int = 0; i < 100; i = i + 1) {
    sum = sum + square(i)
  }
  println("Sum of squares: " + unparseInt(sum))
}
