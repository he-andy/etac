use io
use conv

function isEven(n:int) : bool {
  return n % 2 == 0
}

main(args:int[][]) {
  count:int = 0
  for (i:int = 0; i < 100; i = i + 1) {
    if (isEven(i)) {
      count = count + 1
    }
  }
  println("Count of even numbers: " + unparseInt(count))
}
